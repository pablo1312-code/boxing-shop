# =====================================================================
# PERFIL LOCAL — usar cuando se ejecuta desde Eclipse (sin Docker)
# =====================================================================
# Para activarlo en Eclipse:
#   Run Configurations → Spring Boot → Profiles → escribe: local
# O añade en application.properties: spring.profiles.active=local
# =====================================================================

spring.datasource.url=jdbc:mysql://localhost:3306/boxingshop?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Si tu MySQL local tiene otro usuario/contraseña, cámbialo aquí.
# Puedes ver las credenciales en MySQL Workbench:
#   Edit Connection → Connection → Username
